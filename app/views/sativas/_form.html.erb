<%= form_with(model: sativa, local: true) do |form| %>
  <% if sativa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sativa.errors.count, "error") %> prohibited this sativa from being saved:</h2>

      <ul>
      <% sativa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
